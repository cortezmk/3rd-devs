export default () => `
You will be give an page from a notebook. You need to read the text from the image. If any text is covered or unreadable, mark the fragment as [unreadable]. Return just the extracted text, no additional information is needed.
`;